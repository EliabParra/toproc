# Copy this file to .env and adjust values.
# IMPORTANT: never commit .env with real secrets.

# App
APP_PORT=3000
APP_HOST=localhost
APP_LANG=es

# Reverse proxy / load balancer
# If you deploy behind a proxy (Render/Heroku/Nginx/Cloudflare/etc.) and need secure cookies or real client IPs,
# set Express trust proxy. Common value: 1
#APP_TRUST_PROXY=1

# Frontend (optional)
# Choose who serves pages:
# - none: API-only (decoupled) (recommended default)
# - pages: backend serves legacy HTML from /public/pages
# - spa: backend serves a built SPA (React/Angular/Vue/etc.) from SPA_DIST_PATH
APP_FRONTEND_MODE=none

# Only required when APP_FRONTEND_MODE=spa
# Must point to a folder containing index.html (the build output)
# Examples:
# SPA_DIST_PATH=..\frontend-angular-repo\dist\frontend-angular-repo\browser
# SPA_DIST_PATH=C:\absolute\path\to\some-spa\dist
#SPA_DIST_PATH=

# Optional dev helper (pnpm run full)
# Path to your frontend repo folder (must contain package.json)
# Example:
# FRONTEND_PATH=..\frontend-angular-repo
#FRONTEND_PATH=

# Optional: which pnpm script to run in the frontend when using `pnpm run full`
#FRONTEND_SCRIPT=start

# Optional: extra args passed to the frontend script when using `pnpm run full`
# This becomes: pnpm run <FRONTEND_SCRIPT> -- <FRONTEND_ARGS>
#FRONTEND_ARGS=--port 4200

# Optional: which pnpm script to run in the backend when using `pnpm run full`
#BACKEND_SCRIPT=dev

# Optional: extra args passed to the backend script when using `pnpm run full`
# This becomes: pnpm run <BACKEND_SCRIPT> -- <BACKEND_ARGS>
#BACKEND_ARGS=

# Postgres (either DATABASE_URL or PG* variables)
#DATABASE_URL=postgres://postgres:password@localhost:5432/curso-node
PGHOST=localhost
PGPORT=5432
PGDATABASE=curso-node
PGUSER=postgres
PGPASSWORD=change_me
PGSSL=false

# Auth (optional)
# Controls login identifier behavior and optional new-device 2-step.
#AUTH_LOGIN_ID=email
#AUTH_LOGIN_2STEP_NEW_DEVICE=0
# Optional: enable anonymous /toProccess using a public profile id (permissions still apply)
#AUTH_PUBLIC_PROFILE_ID=2
# Optional: profile id assigned during Auth registration
#AUTH_SESSION_PROFILE_ID=1
# Optional: require email verification before allowing login
#AUTH_REQUIRE_EMAIL_VERIFICATION=0

# Email (optional)
# EMAIL_MODE=log|smtp
# - log: prints email metadata to logs (tokens/codes hidden by default)
# - smtp: sends emails via SMTP_* settings
#EMAIL_MODE=log
#SMTP_HOST=smtp.example.com
#SMTP_PORT=587
#SMTP_USER=change_me
#SMTP_PASS=change_me
#SMTP_SECURE=false
#SMTP_FROM="My App <no-reply@example.com>"

# Session secret rotation
# Use a single secret:
#SESSION_SECRET=change_me
# Or rotate secrets (first is current signer, rest are previous):
SESSION_SECRETS=change_me_current,change_me_previous

# Optional: where the session table lives (connect-pg-simple)
# By default it uses the `public` schema.
#SESSION_SCHEMA=public
#SESSION_TABLE=session

# Optional cookie overrides
#SESSION_COOKIE_SECURE=true
#SESSION_COOKIE_SAMESITE=lax
#SESSION_COOKIE_MAXAGE_MS=1800000

# Optional CORS overrides (handy for production)
#CORS_ENABLED=true
#CORS_CREDENTIALS=true
# Comma-separated list of allowed origins:
#CORS_ORIGINS=https://my-frontend.example,https://admin.example

# Logging
# LOG_FORMAT=text|json
# - text: current human-friendly colored logs
# - json: one-line JSON per event (recommended for production log aggregation)
#LOG_FORMAT=text
